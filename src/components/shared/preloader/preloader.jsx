import React from 'react';
import preloader from '../../../assets/images/preloader.svg';

const Preloader = (props) => {
  return <img src={preloader} alt="preloader" width={20} height={20} />;
};

export default Preloader;